---
# 1. Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: openshift-authorino
  annotations:
    argocd.argoproj.io/sync-wave: "2"
---
# 2. OperatorGroup (Required because we are using a specific namespace)
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: authorino-operator-group
  namespace: openshift-authorino
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  targetNamespaces:
    - openshift-authorino
---
# 3. Operator Subscription
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: authorino-operator
  namespace: openshift-authorino
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  channel: stable-v1 # Standard channel for the Red Hat build of Authorino
  installPlanApproval: Automatic
  name: authorino-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace